services:
  - type: web
    name: mywagtail
    plan: free
    env: python
    buildCommand: |
      poetry install
    startCommand: |
      poetry run python manage.py runserver 0.0.0.0:$PORT
    envVars:
      - key: PORT
        value: "8000"
